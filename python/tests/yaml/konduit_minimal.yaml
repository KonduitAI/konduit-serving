# Minimal Konduit serving YAML
serving:
  http_port: 1337
steps:
  - type: PYTHON
    python_configs:
      default:
        python_path: .
        python_code_path: ./simple.py
        python_inputs:
          first: NDARRAY
        python_outputs:
          second: NDARRAY
client:
  port: 1337